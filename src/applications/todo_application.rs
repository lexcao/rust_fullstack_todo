/*use crate::domains::todo_domain::{Todo, TodoDomain};

struct TodoApplication {
    domain: TodoDomain,
}

impl TodoApplication {
    pub fn new(domain: TodoDomain) -> Self {
        Self { domain }
    }
    pub async fn create(&self, todo: Todo) -> Todo {
        todo!()
    }

    pub fn update(&self, _todo: Todo) -> Todo {
        todo!()
    }

    pub fn delete(&self, _todo: Todo) {
        todo!()
    }

    pub fn update_status(&self, _todo: Todo) -> Todo {
        todo!()
    }
}


#[cfg(test)]
mod tests {
    use std::time::SystemTime;
    use crate::domains::todo_domain::TodoStatus;
    use super::*;

    #[test]
    fn test_application_create_todo() {
        // let domain = TodoDomain::new;
        // let todo_application = TodoApplication::new(domain);
        // let todo = todo_application.create(Todo {
        //     id: 1,
        //     content: "".to_string(),
        //     status: TodoStatus::Todo,
        //     created_at: SystemTime::now(),
        // });
        // assert_eq!(todo.id, 1);
    }
}
*/